[workspace]
name = "llm"
version = "0.1.0"
description = "Unified CLI wrapper for ollama, mlx-lm, and vllm-mlx."
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64", "win-64"]

[dependencies]
python = ">=3.11"

[target.osx-arm64.pypi-dependencies]
pyinstaller = ">=6.0"

[target.osx-arm64.tasks]
build = "python build.py"
